"use strict";
(() => {
    console.log("Any's");
    const sets = 12.734;
    let reps;
    let exercise = 'Curls';
    // Cast or Casting is a way to change a type
    console.log(`(1.) ${sets.toFixed(2)}`);
    console.log(`(2) ${exercise.toUpperCase()}`);
    console.log({
        sets,
        reps,
        exercise
    });
})();
